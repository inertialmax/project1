# project1
**Goal: Refamiliarize myself with pointers and memory**

Write a single C program that:

- defines a struct with at least 4 fields (mix of types)
- dynamically allocates an array of these structs
- populates them from hardcoded data
- prints them cleanly
- frees everything properly